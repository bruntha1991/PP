{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Part</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Partname</th>
                <td>{{ entity.partName }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity.price }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('part') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('part_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
